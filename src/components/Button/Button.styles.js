import styled from "styled-components";

export const Wrapper = styled.button`
  /* display: inline;
  text-align: center;
  width: 100%;
  height: 60px;
  color: var(--white);
  font-size: var(--fontBig);
  margin: 1rem 1rem;
  margin-left: 5rem;
  transition: all 0.3s;
  outline: none;
  cursor: pointer;

  :hover {
    opacity: 0.8;
  } */
`;
